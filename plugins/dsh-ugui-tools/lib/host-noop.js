/**
 * Browser-package host entry.
 *
 * The real host implementation is mounted separately with a revision query in
 * cordis.patch.yml, so this package entry intentionally does nothing on host.
 */
export function apply() {}
